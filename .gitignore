node_modules/
.next/
.vercel/
.venv/
__pycache__/
.pytest_cache/
*.pyc
.env
.env.local
.claude/settings.local.json
next-env.d.ts
*.tsbuildinfo
ik-schedule-*.csv
.data/
.vercel
.env*

# macOS finder metadata — was committed twice by accident
.DS_Store
**/.DS_Store

# Internal context and working notes — kept locally, never published with the source.
# Design context, the dependency graph and the QA report all live here.
docs/

# Regenerated by `next dev` on every run; deliberately not part of the source
AGENTS.md
CLAUDE.md
